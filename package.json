{
  "name": "@larshp/valico-rs-wasm",
  "version": "0.0.1",
  "description": "todo",
  "scripts": {
    "pub_in_pkg": "npm --no-git-tag-version version patch && npm publish --access public",
    "debugbuild": "rm -rf pkg && wasm-pack build --debug --target nodejs && node index.mjs",
    "test": "rm -rf pkg && wasm-pack build --scope larshp --target nodejs && node index.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/abap-wasm/valico-rs-wasm"
  },
  "devDependencies": {
  },
  "author": "Lars Hvam",
  "license": "MIT"
}